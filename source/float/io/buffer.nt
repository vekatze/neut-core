import {
  this.bool,
  this.either {Right},
  this.file.buffer {buffer},
  this.file.buffer.unsafe {append},
  this.system {system},
  this.unit {Unit, unit},
}

foreign {
  neut_core_v0_55_build_float_buf(pointer, int, float, int) -> int,
}

define write-float(b: &buffer, x: float) ->> system(unit) {
  let size = 32;
  let add-newline = 0;
  let buffer-ptr = magic alloca(int, size);
  let len = magic external neut_core_v0_55_build_float_buf(buffer-ptr, size, x, add-newline);
  if gt-int(len, 0) {
    append(b, buffer-ptr, len)
  } else {
    Right(Unit)
  }
}

define write-float-line(b: &buffer, x: float) ->> system(unit) {
  let size = 32;
  let add-newline = 1;
  let buffer-ptr = magic alloca(int, size);
  let len = magic external neut_core_v0_55_build_float_buf(buffer-ptr, size, x, add-newline);
  if gt-int(len, 0) {
    append(b, buffer-ptr, len)
  } else {
    Right(Unit)
  }
}
