data descriptor {}

define stdin(): descriptor {
  magic cast(int, descriptor, 0)
}

define stdout(): descriptor {
  magic cast(int, descriptor, 1)
}

define stderr(): descriptor {
  magic cast(int, descriptor, 2)
}
