import {
  this.bool {bool},
  this.box,
  this.show {Show, show},
  this.string {string},
}

constant as-show: show(bool) {
  Show((b) => {
    if b {
      *"True"
    } else {
      *"False"
    }
  })
}

define show(b: bool) -> string {
  let Show {show} = as-show;
  show(b)
}

define show-bool(b: bool) -> string {
  this.bool.show.show(b)
}
