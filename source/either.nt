import {
  this.unit {Unit},
}

data either(e, a) {
| Left(e)
| Right(a)
}

inline none<a>(): ?a {
  Left(Unit)
}

define from-option<a>(x: a, my: ?a): a {
  match my {
  | Right(y) =>
    y
  | Left(_) =>
    x
  }
}
