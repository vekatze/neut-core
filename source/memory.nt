import {
- this.unit [unit]
}

define store-int(value: int, address: int): unit {
  introspect arch {
  - amd64 =>
    magic store(int64, value, address)
  - arm64 =>
    magic store(int64, value, address)
  }
}

define store-int8(value: int8, address: int): unit {
  magic store(int8, value, address)
}

define store-int1(value: int1, address: int): unit {
  magic store(int1, value, address)
}

define load-int(address: int): int {
  magic load(int64, address)
}

define load-int8(address: int): int8 {
  magic load(int8, address)
}

define load-int1(address: int): int1 {
  magic load(int1, address)
}

define store-float(value: float, address: int): unit {
  magic store(float, value, address)
}

define load-float(address: int): float {
  magic load(float, address)
}
