import {
  this.array {array},
}

constant access-unchecked: _ {
  this.array._access-unchecked
}

constant swap-unchecked: _ {
  this.array._swap-unchecked
}

inline-meta put-unchecked<a>(xs: 'array(a), i: 'int, x: 'a) -> 'array(a) {
  this.array._put-unchecked(xs, i, x)
}
