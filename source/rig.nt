import {
  this.ring {Ring, ring},
}

data rig(a) {
| Rig(
    zero: a,
    add: (a, a) -> a,
    one: a,
    mul: (a, a) -> a,
  )
}

inline from-ring<a>(f: ring(a)): rig(a) {
  let Ring of {zero, add, one, mul} = f in
  Rig of {zero, add, one, mul}
}
