import {
  this.list {Nil, append, list},
  this.monoid {Monoid, monoid},
}

inline as-monoid<a>() -> monoid(list(a)) {
  Monoid{
    e := Nil,
    op := {
      (x, y) => {
        append(x, y)
      }
    },
  }
}
